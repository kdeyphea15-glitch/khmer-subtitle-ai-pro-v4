{
  "name": "khmer-subtitle-ai-pro-v4",
  "version": "1.0.0",
  "description": "Khmer Subtitle AI Pro V4 - AI Dubbing Studio for Khmer",
  "private": true,
  "main": "index.js",
  "scripts": {
    "dev": "concurrently -n client,server -c cyan,magenta \"npm run dev --prefix client\" \"npm run dev --prefix server\"",
    "build": "npm run build --prefix client && npm run build --prefix server",
    "start": "npm run start --prefix server",
    "lint": "npm run lint --prefix client"
  },
  "keywords": [
    "khmer",
    "subtitle",
    "ai",
    "dubbing"
  ],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
